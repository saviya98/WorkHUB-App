<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adverticepro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Worker_form"></activity>
        <activity android:name=".Supplier_form" />
        <activity android:name=".projects" />
        <activity android:name=".viewProject" />
        <activity android:name=".myProjects" />
        <activity android:name=".form" />
        <activity android:name=".newProject" />
        <activity android:name=".AdvertiseHome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>